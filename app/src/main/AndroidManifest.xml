<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.lewisjuggins.headsuphide">

    <application android:allowBackup="true" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Hide Heads Up instead of dismissing the notification." />
        <meta-data
            android:name="xposedminversion"
            android:value="61" />
    </application>

</manifest>
